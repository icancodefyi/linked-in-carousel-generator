// Locked layout configuration for brand consistency
export const layoutConfig = {
  // Slide dimensions
  slideWidth: 1200,
  slideHeight: 1500,
  
  // Spacing
  leftPadding: 140,
  rightPadding: 120,
  topPadding: 160,
  bottomPadding: 80,
  contentWidth: 860,
  verticalSpacing: 48,
  
  // Typography sizes
  typography: {
    hookBlock: 96,
    headline: 60,
    secondaryBlocks: 72,
    closingLine: 34,
    footerName: 22,
    footerHandle: 18,
    pageNumber: 16,
  },
  
  // Colors
  colors: {
    blueBlock: '#10348C',
    textDark: '#0F172A',
    textMuted: '#64748B',
    gridOverlay: '#F3F4F6',
    background: '#FFFFFF',
  },
  
  // Font families
  fonts: {
    all: 'var(--font-inter-tight), system-ui, -apple-system, sans-serif',
  },
};
